# 🚀 Video Rhythm Cam 部署指南

本指南将帮助你将 Video Rhythm Cam Web 应用部署到互联网上，让全世界的用户都能访问。

## 📋 部署方案概览

### 当前实现（已完成）
- ✅ 现代化首页展示
- ✅ 功能介绍和使用指南
- ✅ 响应式设计
- ✅ 支持在线预览（Web 应用界面）

### 三种部署方式

| 方式 | 难度 | 成本 | 功能 | 推荐度 |
|------|------|------|------|--------|
| **Vercel/Netlify** | ⭐ 简单 | 💰 免费 | 静态网站 + 预览 | ⭐⭐⭐⭐⭐ |
| **Railway/Render** | ⭐⭐ 中等 | 💰💰 $5-20/月 | 完整后端处理 | ⭐⭐⭐⭐ |
| **自建服务器** | ⭐⭐⭐ 复杂 | 💰💰💰 $5+/月 | 完全控制 | ⭐⭐⭐ |

---

## 🎯 方案 1：Vercel 部署（推荐 - 免费）

Vercel 是 Next.js 官方推荐的部署平台，提供免费额度，非常适合展示网站。

### 步骤 1：准备 GitHub 仓库

```bash
# 1. 初始化 git 仓库（如果还没有）
cd /Users/a123/AI/pipi/video-rhythm-cam
git init

# 2. 添加所有文件
git add .

# 3. 创建首次提交
git commit -m "feat: 初始化 Video Rhythm Cam Web 应用"

# 4. 在 GitHub 创建新仓库（访问 github.com）
# 仓库名: video-rhythm-cam

# 5. 关联远程仓库
git remote add origin https://github.com/你的用户名/video-rhythm-cam.git

# 6. 推送到 GitHub
git branch -M main
git push -u origin main
```

### 步骤 2：部署到 Vercel

#### 方法 A：通过 Vercel 网站（推荐）

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 使用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add New" → "Project"
   - 选择你的 `video-rhythm-cam/web` 目录
   - 点击 "Import"

3. **配置项目**
   ```json
   {
     "Framework Preset": "Next.js",
     "Root Directory": "web",
     "Build Command": "npm run build",
     "Output Directory": ".next"
   }
   ```

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待 2-3 分钟
   - 部署完成后会获得一个 URL（如 `https://video-rhythm-cam.vercel.app`）

#### 方法 B：通过 Vercel CLI

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录
vercel login

# 3. 部署（在 web 目录下）
cd web
vercel

# 4. 按提示操作
# - 登录 GitHub
# - 选择项目
# - 设置项目名称
```

### 步骤 3：配置自定义域名（可选）

1. 在 Vercel 项目设置中
2. 点击 "Domains"
3. 添加你的域名（如 `video-rhythm-cam.com`）
4. 按照提示配置 DNS 记录

### 步骤 4：更新代码自动部署

```bash
# 修改代码后
git add .
git commit -m "feat: 更新功能"
git push

# Vercel 会自动检测并重新部署！
```

---

## 🎯 方案 2：Netlify 部署（免费）

Netlify 是另一个优秀的免费托管平台。

### 步骤 1：准备项目

在 `web/` 目录创建 `netlify.toml`：

```toml
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
```

### 步骤 2：部署

1. 访问 https://netlify.com
2. 使用 GitHub 账号登录
3. 点击 "Add new site" → "Import an existing project"
4. 选择你的 GitHub 仓库
5. 构建命令：`npm run build`（在 `web` 目录）
6. 发布目录：`.next`
7. 点击 "Deploy site"

---

## 🎯 方案 3：Railway 部署（付费 - 完整后端）

如果你需要完整的服务器端处理能力（视频上传、处理、下载），Railway 是个不错的选择。

### 定价
- **免费计划**：$5/月额度
- **付费计划**：$5-20/月

### 步骤 1：创建 Railway 账号

1. 访问 https://railway.app
2. 使用 GitHub 账号登录
3. 添加支付方式

### 步骤 2：部署 Web 应用

```bash
# 安装 Railway CLI
npm i -g railway

# 登录
railway login

# 初始化项目
cd web
railway init

# 部署
railway up
```

### 步骤 3：配置环境变量

在 Railway 控制台设置：
- `NODE_ENV=production`
- `PORT=3000`

---

## 🔧 环境变量配置

在部署平台添加以下环境变量（可选）：

```bash
# .env.production
NEXT_PUBLIC_SITE_URL=https://video-rhythm-cam.vercel.app
NEXT_PUBLIC_GITHUB_URL=https://github.com/remotion-dev/remotion
NEXT_PUBLIC_VERSION=2.0.0
```

---

## 📊 部署后功能对比

| 功能 | Vercel | Netlify | Railway |
|------|--------|---------|---------|
| 静态网站 ✅ | ✅ | ✅ | ✅ |
| 服务器渲染 ✅ | ✅ | ✅ | ✅ |
| API 路由 ✅ | ✅ | ✅ | ✅ |
| 视频上传 ❌ | ❌ | ❌ | ✅ |
| 视频处理 ❌ | ❌ | ❌ | ✅ |
| 文件存储 ❌ | ❌ | ❌ | ✅ |
| WebSocket ✅ | ✅ | ❌ | ✅ |
| 成本 | 免费 | 免费 | $5+/月 |

---

## 🎨 自定义域名

### 购买域名
- Namecheap: https://www.namecheap.com
- GoDaddy: https://www.godaddy.com
- Cloudflare Registrar: https://www.cloudflare.com/products/registrar/

### 配置 DNS

在域名提供商添加 CNAME 记录：

```
Type: CNAME
Name: @
Value: cname.vercel-dns.com (或你的托管平台)
```

---

## 🔍 部署检查清单

部署前确认：

- [ ] GitHub 仓库已创建并推送
- [ ] `web/package.json` 依赖完整
- [ ] `web/vercel.json` 配置正确
- [ ] 本地构建成功（`npm run build`）
- [ ] 环境变量已配置（如需要）

部署后验证：

- [ ] 网站可以访问
- [ ] 所有页面正常加载
- [ ] 响应式设计在移动端正常
- [ ] 在线体验页面可以打开
- [ ] 所有链接都有效

---

## 🐛 常见问题

### 1. 构建失败

**问题**：Vercel 构建时报错

**解决方案**：
```bash
# 本地测试构建
cd web
npm run build

# 检查依赖
npm install

# 清除缓存重新部署
# 在 Vercel 项目设置中点击 "Redeploy"
```

### 2. 页面 404

**问题**：某些页面返回 404

**解决方案**：
- 确保 `app/` 目录下所有页面都存在
- 检查路由配置
- 清除浏览器缓存

### 3. 样式丢失

**问题**：Tailwind 样式没有加载

**解决方案**：
- 检查 `tailwind.config.ts` 配置
- 确保 `globals.css` 正确引入
- 检查 `next.config.js` 配置

### 4. 部署太慢

**问题**：首次加载时间过长

**解决方案**：
- 优化图片（使用 Next.js Image）
- 启用 CDN（Vercel 自动启用）
- 减少初始加载体积

---

## 📈 性能优化

### 1. 启用图片优化

```tsx
import Image from 'next/image';

<Image
  src="/hero.jpg"
  alt="Hero"
  width={1920}
  height={1080}
  priority
/>
```

### 2. 配置缓存

```javascript
// next.config.js
module.exports = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=31536000, immutable',
          },
        ],
      },
    ];
  },
};
```

### 3. 启用压缩

Vercel 自动启用 gzip 和 brotli 压缩。

---

## 🔐 安全建议

1. **HTTPS**：Vercel/Netlify 自动提供
2. **环境变量**：不要在代码中硬编码密钥
3. **CORS**：配置正确的跨域策略
4. **Rate Limiting**：防止 API 滥用

---

## 📚 参考资源

- [Vercel 文档](https://vercel.com/docs)
- [Next.js 部署文档](https://nextjs.org/docs/deployment)
- [Netlify 文档](https://docs.netlify.com)
- [Railway 文档](https://docs.railway.app)

---

## 🎉 下一步

部署成功后：

1. **分享你的网站**
   - 在社交媒体分享链接
   - 添加到 GitHub README
   - 提交到产品目录

2. **收集反馈**
   - 添加分析工具（Google Analytics）
   - 收集用户反馈
   - 监控性能

3. **持续改进**
   - 修复 bug
   - 添加新功能
   - 优化用户体验

祝部署顺利！🚀
