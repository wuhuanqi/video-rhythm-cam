import type { TRenderAsset } from 'remotion';
export declare const onlyInlineAudio: ({ assets, fps, frame, }: {
    assets: TRenderAsset[];
    fps: number;
    frame: number;
}) => AudioData | null;
